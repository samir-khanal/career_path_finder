# 🏗️ Project Architecture Diagram

## System Architecture Overview

```
┌──────────────────────────────────────────────────────────────────┐
│                         USER INTERFACE                           │
│                    (Streamlit Frontend)                          │
│                                                                  │
│  ┌────────────┐  ┌────────────┐  ┌────────────┐                │
│  │   Auth     │  │  Upload &  │  │ Analytics  │                │
│  │   Page     │  │  Analyze   │  │ Dashboard  │                │
│  └────────────┘  └────────────┘  └────────────┘                │
│                                                                  │
│  app/streamlit_app_enhanced.py                                  │
└──────────────────────────────────────────────────────────────────┘
                             │
                             ├─────────────┬─────────────┐
                             ▼             ▼             ▼
┌──────────────────────────────────────────────────────────────────┐
│                      BUSINESS LOGIC LAYER                        │
│                                                                  │
│  ┌──────────────────┐    ┌──────────────────┐                  │
│  │  Parsing Module  │    │  ML/AI Module    │                  │
│  │                  │    │                  │                  │
│  │  • PDF Parser    │    │  • Skill Matcher │                  │
│  │  • DOCX Parser   │    │  • Role Predictor│                  │
│  │  • Text Cleaner  │    │  • Gap Analyzer  │                  │
│  │  • OCR Support   │    │                  │                  │
│  └──────────────────┘    └──────────────────┘                  │
│                                                                  │
│  src/parsing/              src/parsing/ml/                      │
└──────────────────────────────────────────────────────────────────┘
                             │
                             ▼
┌──────────────────────────────────────────────────────────────────┐
│                      DATA ACCESS LAYER                           │
│                                                                  │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐          │
│  │    Auth      │  │   Resume     │  │    Skill     │          │
│  │  Service     │  │  Repository  │  │  Repository  │          │
│  │              │  │              │  │              │          │
│  │ • Sign Up    │  │ • Save       │  │ • Get Roles  │          │
│  │ • Sign In    │  │ • Get List   │  │ • Get Skills │          │
│  │ • Sign Out   │  │ • Delete     │  │ • Search     │          │
│  │ • Get User   │  │ • Analytics  │  │ • Add Custom │          │
│  └──────────────┘  └──────────────┘  └──────────────┘          │
│                                                                  │
│  src/database/                                                  │
└──────────────────────────────────────────────────────────────────┘
                             │
                             ▼
┌──────────────────────────────────────────────────────────────────┐
│                    DATABASE LAYER (Supabase)                     │
│                         PostgreSQL                               │
│                                                                  │
│  ┌─────────┐  ┌─────────┐  ┌───────────┐  ┌───────────┐        │
│  │ users   │  │ resumes │  │ job_roles │  │skill_gaps │        │
│  ├─────────┤  ├─────────┤  ├───────────┤  ├───────────┤        │
│  │ id      │  │ id      │  │ id        │  │ id        │        │
│  │ email   │  │ user_id │  │ role_name │  │ resume_id │        │
│  │ name    │  │ filename│  │ skills    │  │ user_id   │        │
│  │ created │  │ parsed  │  │ category  │  │ matched   │        │
│  └─────────┘  └─────────┘  └───────────┘  └───────────┘        │
│                                                                  │
│  ┌──────────────┐                                               │
│  │skills_database│                                              │
│  ├──────────────┤                                               │
│  │ id           │                                               │
│  │ skill_name   │                                               │
│  │ category     │                                               │
│  │ synonyms     │                                               │
│  │ popularity   │                                               │
│  └──────────────┘                                               │
│                                                                  │
│  + Row Level Security (RLS)                                     │
│  + Authentication via Supabase Auth                             │
└──────────────────────────────────────────────────────────────────┘
```

---

## Data Flow Diagram

### 1. Resume Upload & Analysis Flow

```
┌──────────┐
│  User    │
│ Uploads  │
│ Resume   │
└────┬─────┘
     │
     ▼
┌─────────────────────┐
│ Streamlit Frontend  │
│ (File Upload)       │
└──────┬──────────────┘
       │
       ▼
┌─────────────────────────────────────────┐
│ PDF/DOCX Parser                         │
│                                         │
│ Strategy 1: pdftotext ───┐             │
│ Strategy 2: PyPDF2    ───┼──→ Success? │
│ Strategy 3: pdfplumber───┤             │
│ Strategy 4: OCR ─────────┘             │
└──────┬──────────────────────────────────┘
       │ Raw Text
       ▼
┌─────────────────────────────┐
│ Text Cleaner & Preprocessor │
└──────┬──────────────────────┘
       │ Cleaned Text
       ▼
┌─────────────────────────────┐
│ Enhanced Parser             │
│ • Extract Skills            │
│ • Extract Education         │
│ • Extract Experience        │
└──────┬──────────────────────┘
       │ Structured Data
       ▼
┌─────────────────────────────┐
│ Skill Matcher (DB)          │
│ • Load Job Roles from DB    │
│ • Normalize Skills          │
│ • Match with Synonyms       │
│ • Calculate Gaps            │
└──────┬──────────────────────┘
       │ Analysis Results
       ▼
┌─────────────────────────────┐
│ Save to Database            │
│ • Resume Record             │
│ • Skill Gap Analysis        │
└──────┬──────────────────────┘
       │
       ▼
┌─────────────────────────────┐
│ Display Results             │
│ • Match Score               │
│ • Matched Skills            │
│ • Missing Skills            │
│ • Visualizations            │
└─────────────────────────────┘
```

---

## Database Schema Relationships

```
┌─────────────────┐
│     users       │
│─────────────────│
│ PK: id (uuid)   │
│    email        │
│    full_name    │
│    created_at   │
│    last_login   │
└────┬────────────┘
     │
     │ 1:N
     │
     ├──────────────────────────────┐
     │                              │
     ▼                              ▼
┌─────────────────┐       ┌─────────────────┐
│    resumes      │       │   skill_gaps    │
│─────────────────│       │─────────────────│
│ PK: id          │       │ PK: id          │
│ FK: user_id     │       │ FK: user_id     │
│    filename     │◄──────│ FK: resume_id   │
│    parsed_data  │  1:N  │    target_role  │
│    upload_date  │       │    matched      │
└─────────────────┘       │    missing      │
                          │    match_score  │
                          └─────────────────┘

┌─────────────────────┐
│     job_roles       │
│─────────────────────│
│ PK: id              │
│    role_name        │
│    required_skills  │
│    category         │
│    experience_level │
└─────────────────────┘

┌──────────────────────┐
│  skills_database     │
│──────────────────────│
│ PK: id               │
│    skill_name        │
│    category          │
│    synonyms (jsonb)  │
│    popularity_score  │
└──────────────────────┘
```

---

## Module Dependencies

```
app/streamlit_app_enhanced.py
    │
    ├── src.database
    │   ├── AuthService
    │   ├── ResumeRepository
    │   └── SkillRepository
    │
    └── src.parsing.ml
        └── skill_matcher_db

src/parsing/ml/skill_matcher_db.py
    │
    ├── src.parsing
    │   └── parse_resume()
    │
    └── src.database
        └── SkillRepository

src/parsing/__init__.py
    │
    ├── pdf_parser_improved
    │   └── CrossPlatformPDFExtractor
    │
    ├── docx_parser
    │   └── extract_text_from_docx()
    │
    ├── enhanced_parser
    │   └── enhanced_extract_sections()
    │
    └── text_cleaner
        └── clean_extracted_text()

src/database/
    │
    ├── supabase_client.py
    │   └── get_supabase_client()
    │
    ├── auth_service.py
    │   └── AuthService class
    │
    ├── resume_repository.py
    │   └── ResumeRepository class
    │
    └── skill_repository.py
        └── SkillRepository class
```

---

## Security Architecture

```
┌─────────────────────────────────────────────────────────┐
│                  Security Layers                        │
└─────────────────────────────────────────────────────────┘

Layer 1: Authentication
├── Supabase Auth
├── Email/Password
├── JWT Tokens
└── Session Management

Layer 2: Authorization
├── Row Level Security (RLS)
│   ├── Users can only view own data
│   ├── auth.uid() checks
│   └── Policies per table
└── API Key Security

Layer 3: Data Protection
├── Encrypted connections (HTTPS)
├── Password hashing
├── SQL injection prevention
└── Input validation

Layer 4: Application Security
├── Error handling
├── No data leakage
├── Secure file uploads
└── Session timeouts
```

---

## Deployment Architecture

```
┌──────────────────────────────────────────────┐
│            Production Deployment              │
└──────────────────────────────────────────────┘

┌─────────────┐
│   Streamlit │  ← Web Application
│    Cloud    │     (Frontend + Backend)
└──────┬──────┘
       │
       │ HTTPS
       ▼
┌─────────────┐
│  Supabase   │  ← Database & Auth
│   Cloud     │     (PostgreSQL + Auth)
└─────────────┘

Alternative Local Deployment:
┌─────────────┐
│ Local Host  │
│ localhost:  │
│   8501      │
└──────┬──────┘
       │
       │ API Calls
       ▼
┌─────────────┐
│  Supabase   │
│   Cloud     │
└─────────────┘
```

---

## File Upload & Processing Pipeline

```
User Uploads File
      │
      ▼
┌──────────────────┐
│ Validate File    │
│ • Check type     │
│ • Check size     │
└────┬─────────────┘
     │
     ▼
┌──────────────────┐
│ Save Temporarily │
│ app/uploads/     │
└────┬─────────────┘
     │
     ▼
┌──────────────────────────────┐
│ Extract Text                 │
│ ┌─────────────────────────┐  │
│ │ Try pdftotext           │  │
│ │   ↓ failed?             │  │
│ │ Try PyPDF2              │  │
│ │   ↓ failed?             │  │
│ │ Try pdfplumber          │  │
│ │   ↓ failed?             │  │
│ │ Try OCR (Tesseract)     │  │
│ └─────────────────────────┘  │
└────┬─────────────────────────┘
     │
     ▼
┌──────────────────┐
│ Parse Sections   │
│ • Skills         │
│ • Education      │
│ • Experience     │
└────┬─────────────┘
     │
     ▼
┌──────────────────┐
│ Analyze Skills   │
│ • Match roles    │
│ • Find gaps      │
│ • Score          │
└────┬─────────────┘
     │
     ▼
┌──────────────────┐
│ Save to Database │
│ • Resume data    │
│ • Analysis       │
└────┬─────────────┘
     │
     ▼
┌──────────────────┐
│ Display Results  │
│ • UI update      │
│ • Charts         │
└────┬─────────────┘
     │
     ▼
┌──────────────────┐
│ Cleanup          │
│ Delete temp file │
└──────────────────┘
```

---

## UI Component Hierarchy

```
StreamlitApp
│
├── init_session_state()
│   ├── authenticated
│   ├── user
│   ├── auth_service
│   ├── resume_repo
│   └── skill_repo
│
├── show_auth_page()
│   ├── Tab: Sign In
│   │   └── Form: email, password
│   └── Tab: Sign Up
│       └── Form: name, email, password
│
└── show_dashboard()
    │
    ├── Header (with Sign Out button)
    │
    ├── Statistics Cards
    │   ├── Total Resumes
    │   ├── Analyses Done
    │   ├── Avg Match Score
    │   └── Unique Skills
    │
    └── Tabs
        │
        ├── Upload & Analyze
        │   ├── File Uploader
        │   ├── Column 1: Extracted Info
        │   │   ├── Skills (badges)
        │   │   ├── Education
        │   │   └── Experience
        │   └── Column 2: Analysis
        │       ├── Role Predictions
        │       ├── Target Role Selector
        │       ├── Match Score
        │       ├── Matched Skills
        │       └── Missing Skills
        │
        ├── My Resumes
        │   └── List of Resumes (expandable)
        │       ├── Metadata
        │       ├── Skills
        │       └── Delete Button
        │
        └── Analytics
            ├── Match Score Trend (line chart)
            ├── Role Distribution (pie chart)
            └── Top Analyses (table)
```

---

## Technology Stack Layers

```
┌─────────────────────────────────────────┐
│         Presentation Layer              │
│  • Streamlit                            │
│  • Custom CSS/HTML                      │
│  • Plotly Charts                        │
└─────────────────────────────────────────┘
                  │
┌─────────────────────────────────────────┐
│         Application Layer               │
│  • Python 3.8+                          │
│  • Business Logic                       │
│  • Data Processing                      │
└─────────────────────────────────────────┘
                  │
┌─────────────────────────────────────────┐
│         Service Layer                   │
│  • Authentication Service               │
│  • Resume Repository                    │
│  • Skill Repository                     │
└─────────────────────────────────────────┘
                  │
┌─────────────────────────────────────────┐
│         Data Access Layer               │
│  • Supabase Client                      │
│  • PostgreSQL Queries                   │
│  • ORM Operations                       │
└─────────────────────────────────────────┘
                  │
┌─────────────────────────────────────────┐
│         External Services               │
│  • Supabase (Database + Auth)           │
│  • Tesseract OCR                        │
│  • Poppler (PDF tools)                  │
└─────────────────────────────────────────┘
```

---

## Key Design Patterns Used

### 1. Repository Pattern
```
ResumeRepository
├── save_resume()
├── get_user_resumes()
├── delete_resume()
└── get_resume_statistics()
```

### 2. Singleton Pattern
```
supabase_client.py
└── get_supabase_client() ← Returns same instance
```

### 3. Strategy Pattern
```
PDF Extraction Strategies:
├── Strategy 1: pdftotext
├── Strategy 2: PyPDF2
├── Strategy 3: pdfplumber
└── Strategy 4: OCR
```

### 4. Facade Pattern
```
parse_resume(file_path)
├── Hides complexity of:
│   ├── PDF extraction
│   ├── Text cleaning
│   └── Section parsing
└── Returns: Simple dict
```

---

## Error Handling Flow

```
┌────────────────┐
│  User Action   │
└───────┬────────┘
        │
        ▼
┌────────────────┐
│  Try Block     │
│  Main Logic    │
└───────┬────────┘
        │
        ├─ Success ──→ Continue
        │
        └─ Error ──→ ┌────────────────┐
                     │  Except Block  │
                     │  Log Error     │
                     │  Show Message  │
                     └───────┬────────┘
                             │
                             ▼
                     ┌────────────────┐
                     │  Graceful      │
                     │  Degradation   │
                     │  or Fallback   │
                     └────────────────┘
```

---

This architecture ensures:
- ✅ Separation of concerns
- ✅ Scalability
- ✅ Maintainability
- ✅ Security
- ✅ Testability
- ✅ User-friendly experience
